{
  "version": "1.0.13",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/main.js --platform=node --target=node20 --minify",
    "test": "jest"
  },
  "devDependencies": {
    "esbuild": "0.27.3",
    "jest": "^30.2.0"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "volta": {
    "node": "20.18.3"
  }
}
